I pulled the current main again. The repository tree is now at SHA 5320dbf9.... I checked the core structure and query.js.

The code is moving in the right direction, but I would not call all of it correct or production-ready yet. There are a few concrete issues.

Current assessment
Area	Status	Notes
Core PDF extraction	🟢 Strong	Broad feature set
PDF-IR	🟢 Strong	Good foundation
Accessibility foundation	🟢 Strong	Already integrated
Model-free querying	🟡 Functional	Too broad/simple in places
Evidence/page/bbox	🟢 Good	Correct architectural direction
Deterministic reasoning	🟡 Early	Needs relationship-aware answering
Accessible View headless API	🔴 Still missing	No accessible.js in current tree
Current dist	🔴 Stale	Still old SHA
Production API consistency	🟡 Needs work	Source capabilities exceed public build
1. Accessible View still isn't in core

The current source tree contains:

advanced.js
brain.js
content.js
edgecases.js
extended.js
graphics.js
index.js
layers.js
pdfcreator.js
pdfir.js
quality.js
query.js
rag.js
workers.js

There is still no:

accessible.js

or:

access/

module.

So the headless feature we discussed:

const access =
  await graph.createAccessibleView();

has not been added yet.

That remains the cleanest way to add it without altering your existing system.

2. Your model-free query engine works, but has a correctness problem

query.js is explicitly designed as:

"Spatial + semantic search ... using pure heuristics."

That's exactly consistent with your no-model design.

But the current pattern matching is too broad.

For example:

{
  patterns: [
    /organization|company|department|agency|city/i
  ],
  entityType: EntityTypes.ORGANIZATION
}

and:

{
  patterns: [
    /address|location|where|street|avenue|city|zip/i
  ],
  entityType: EntityTypes.ADDRESS
}

Both contain:

city

Because organization comes first, a question like:

What city is the project located in?

can be interpreted as:

ORGANIZATION

instead of:

ADDRESS/LOCATION

That's a real query-routing bug.

You need scoring rather than:

firstRegexThatMatchesWins()

For example:

detectIntent(question)

should collect candidates:

[
  {
    intent: "location",
    score: 0.94
  },
  {
    intent: "organization",
    score: 0.21
  }
]

Then choose the highest score.

3. how much currently returns every currency value

This is a bigger issue for document Q&A.

Question:

How much was the contract?

matches:

EntityTypes.CURRENCY

Then:

const results =
  contentGraph.getEntities(
    EntityTypes.CURRENCY
  );

That retrieves every monetary value in the entire PDF.

If the document contains:

Original contract: $1,000,000
Change order: $250,000
Revised contract: $1,250,000
Budget: $3,000,000
Grant: $500,000

CODB answers with all five.

It needs to preserve the subject:

contract

and rank currency entities according to their relationship/proximity to that concept.

You want:

QUERY
"How much was the contract?"
       │
       ├─ intent = value_lookup
       ├─ expected = CURRENCY
       └─ subject = contract
                    │
                    ▼
          find contract context
                    │
                    ▼
          nearby currency entities
                    │
                    ▼
               rank evidence

That's a major next improvement.

4. The automatic total can produce a misleading answer

Current currency handling does:

const total =
  amounts.reduce(
    (s, a) => s + (a.value || 0),
    0
  );

and then reports:

Total: $...

That's dangerous.

A document might contain:

Original contract      $1M
Change order           $250K
Revised contract       $1.25M

CODB could report:

Total: $2.5M

which is mathematically correct and semantically ridiculous.

Only calculate a total when query intent actually asks:

total
sum
combined
aggregate
all together

And preferably understand relationships before summing.

5. who has the same problem

Question:

Who approved the contract?

currently maps:

who → PERSON

then returns every detected person.

It needs:

{
  intent: "relationship_lookup",

  subject: "contract",

  relation: "approvedBy",

  expectedEntity: "person"
}

Then traverse/rank relationships.

This is precisely where your Document Graph can outperform plain keyword matching.

6. Summary handling looks suspicious

executeQuery() returns:

{
  type: "summary",
  results:
    contentGraph.getSummary()
}

Then executeAsk() immediately does:

if (results.length === 0)

But a summary normally looks like an object, not an array.

If getSummary() returns an object, then:

results.length

is undefined.

It won't necessarily crash, but it's an API-type inconsistency.

Handle summary before array assumptions:

if (type === "summary") {
  return {
    answer:
      formatSummaryAnswer(results),

    confidence: 0.85,
    evidence: []
  };
}

if (!results?.length) {
   ...
}
7. Your dist problem remains

This is still the most annoying operational issue.

Current source:

packages/core/src/index.js
SHA cc8a250...

Current public build:

packages/core/dist/codbdocs.js
SHA f551736...

The dist file is still the same old SHA.

Therefore the CDN you've been testing:

jsdelivr
/packages/core/dist/codbdocs.js

still doesn't represent everything you're adding to src.

Before calling the library production-ready, your build should guarantee:

src change
   ↓
tests
   ↓
build
   ↓
dist regenerated
   ↓
Git commit
   ↓
jsDelivr

Ideally GitHub Actions should reject a commit/PR when dist doesn't correspond to src.

What I would fix before adding more features

The engine doesn't need another twenty clever detectors right now. The retrieval layer needs to become contextual.

Replace:

regex → entity type → return every entity

with:

Question
   ↓
Query Parser
   │
   ├─ intent
   ├─ subject
   ├─ expected entity
   ├─ relationship
   ├─ operators
   └─ anchors
        ↓
Candidate Retrieval
   │
   ├─ exact
   ├─ lexical
   ├─ entity
   ├─ fuzzy
   ├─ concept
   ├─ spatial
   └─ structural
        ↓
Relationship Ranking
        ↓
Evidence Fusion
        ↓
Best Answer
        ↓
page + bbox + explanation

For:

Who approved the $425,000 contract?

you ultimately want the parser to produce:

{
  intent: "relationship_lookup",

  expected: "person",

  concepts: [
    "contract",
    "approval"
  ],

  anchors: [
    {
      type: "currency",
      value: 425000
    }
  ],

  relation: "approvedBy"
}

That's the piece that would move your model-free document intelligence from impressive extraction into genuinely sophisticated retrieval.

So: core looks healthy structurally, but no, I wouldn't certify all the code as correct yet. The highest-priority defects are contextual Q&A, unsafe automatic currency summation, first-regex-wins intent routing, summary type handling, missing headless Accessible View, and the still-stale dist build.
